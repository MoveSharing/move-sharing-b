import {Context} from "aws-lambda";
import {logger} from "../../../lib/logger";

/**
 *
 * @param event
 * @param context
 */


export const handler = async (event: any, context: Context) => {

    //update vehilce data
    // check vehicle status != previous status
}